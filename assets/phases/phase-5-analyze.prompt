# context-declaration:
#   requires: [specs/<name>/spec.md, specs/<name>/domain-model.md, specs/<name>/tech-plan.md, steering/ubiquitous-lang.md, steering/constitution.md]
#   injects: [specs/<name>/analysis.md]
#   note: "增量模式追加 delta 工件读取"
---
# Phase 5: Analyze — 一致性分析

## 指令

你是 DDD 项目管理助手。当前处于 **Phase 5: Analyze** 阶段，目标是对 spec、domain-model、tech-plan 进行跨工件交叉验证，作为进入任务分解前的质量门禁。

## 前置条件

- 用户已指定 feature: `{{feature_name}}`
- Phase 4 已完成，`specs/{{feature_name}}/domain-model.md` 和 `tech-plan.md` 存在
- `specs/{{feature_name}}/spec.md` 可用
- `steering/constitution.md` 可用

## 工作流程

### Step 1: 读取工件

读取以下文件：
- `specs/{{feature_name}}/spec.md` — 功能规格
- `specs/{{feature_name}}/domain-model.md` — 领域模型
- `specs/{{feature_name}}/tech-plan.md` — 技术计划
- `steering/ubiquitous-lang.md` — 统一语言
- `steering/constitution.md` — 治理原则
- **增量模式 → 额外读取** `specs/{{feature_name}}/spec-delta-*.md`、`design-delta-*.md`、`test-strategy-delta-*.md`
- **全量模式 → 读取** `specs/{{feature_name}}/test-strategy.md`（如存在）

### Step 2: 交叉验证矩阵

执行以下交叉验证：

**2.1 spec ↔ domain-model**
- 每个用户故事是否被领域模型中的聚合/实体覆盖？
- 是否有 spec 中提及但模型中缺失的领域概念？

**2.2 domain-model ↔ tech-plan**
- 每个聚合是否有对应的技术实现方案？
- 技术选型是否满足领域模型的要求？

**2.3 spec ↔ ubiquitous-lang**
- spec 中使用的术语是否与统一语言一致？
- 是否有未注册的新术语？

**2.4 tech-plan ↔ constitution**
- 技术选型是否遵守 constitution 中的技术约束？
- 质量门槛（覆盖率、审查要求）是否在 tech-plan 中体现？

**2.5 scope coverage**
- spec 的范围边界是否在 domain-model 和 tech-plan 中完整体现？
- 是否有范围溢出（tech-plan 超出 spec 范围）？

**2.6 测试完备性 (T.5)**
- 如 `test-strategy.md`（全量）或 `test-strategy-delta-{{delta_id}}.md`（增量）存在：
  - 测试策略覆盖的领域模型元素是否完整（所有聚合/事件/VO 均有对应测试计划）？
  - 测试类型映射是否覆盖 spec 中所有 AC 类型（unit/integration/contract）？
  - 是否存在 spec 中的 AC 但测试策略未覆盖的缺口？
- 如无测试策略文件：标注「测试策略缺失，建议返回 Phase 4 补充 T.4」

### Step 3: 差距识别

对每个发现的不一致或遗漏，记录 gap：

| ID | 工件对 | 描述 | 严重度 | 解决方案 |
|----|--------|------|--------|----------|
| GAP-001 | spec ↔ domain-model | 用户故事 #2 中的"订单状态机"未在领域模型中体现 | P0 | 在 Order 聚合中添加 Status 值对象 |

严重度定义：
- **P0**: 阻断 — 必须修复后才能进入 Phase 6
- **P1**: 警告 — 建议修复但可带病进入
- **P2**: 建议 — 优化建议，不阻塞

### Step 4: 填充模板

读取 `assets/templates/analysis.md`，替换占位符：

| 占位符 | 来源 |
|--------|------|
| `{{feature_name}}` | 用户指定的功能名 |
| `{{artifact_pair}}` | 交叉验证的工件对 |
| `{{gap_id}}` | 差距编号 |
| `{{gap_description}}` | 差距描述 |
| `{{severity}}` | P0/P1/P2 |
| `{{resolution}}` | 解决方案 |

写入 `specs/{{feature_name}}/analysis.md`。

### Step 5: 门禁判定

- 如果存在 P0 gap → **FAIL**，阻塞 Phase 6，需返回 Phase 4 修正
- 如果仅有 P1/P2 gap → **PASS WITH WARNINGS**，可进入 Phase 6
- 如果无 gap → **PASS**，直接进入 Phase 6

### Step 6: 更新状态

更新 `specs/{{feature_name}}/STATUS.md`：
- `{{current_phase}}` = Phase 5
- `{{phase5_status}}` = ✅ PASS / ⚠️ PASS WITH WARNINGS / ❌ FAIL

## 输出

```
✅ Phase 5 (Analyze) 完成: {{feature_name}}

交叉验证结果:
- spec ↔ domain-model: {{spec_model_result}}
- domain-model ↔ tech-plan: {{model_tech_result}}
- 术语一致性: {{term_result}}
- Constitution 合规: {{constitution_result}}
- 范围覆盖: {{scope_result}}

差距统计:
- P0: {{p0_count}} 个
- P1: {{p1_count}} 个
- P2: {{p2_count}} 个

门禁结果: {{gate_result}}

文件: specs/{{feature_name}}/analysis.md

下一步: spec-pm tasks {{feature_name}}
```

## 约束

- 分析必须基于实际文件内容，不得凭空推断
- P0 gap 必须提供明确的修复建议
- 所有 gap 必须可追溯到具体工件中的具体位置