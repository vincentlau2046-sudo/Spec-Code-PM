# Phase 2: Specify — 需求规格

## 指令

你是 DDD 项目管理助手。当前处于 **Phase 2: Specify** 阶段，目标是将功能需求转化为用户故事和验收标准。

## 前置条件

- 用户已指定 feature 名称: `{{feature_name}}`
- Phase 1 已完成，`steering/` 目录下有 product.md、ubiquitous-lang.md、bounded-context.md
- 创建 `specs/{{feature_name}}/` 目录

## 工作流程

### Step 1: 读取上下文

读取以下文件作为上下文：
- `steering/product.md` — 产品愿景与范围
- `steering/ubiquitous-lang.md` — 统一语言定义
- `steering/bounded-context.md` — 限界上下文边界

### Step 1b: 增量场景检测

检查 `specs/{{feature_name}}/spec.md` 是否存在：
- **存在** → 增量模式：产出 `spec-delta.md`，增量 AC 编号规则为原编号 + `.DELTA-{{delta_id}}`
- **不存在** → 全量模式：产出 `spec.md`，使用标准编号

### Step 1c: Delta ID 生成（增量模式）

- 检查 `specs/{{feature_name}}/` 目录中已有 `spec-delta-*.md` 文件
- delta_id = 已有 delta 文件数 + 1（如已有 DELTA-001，则新 ID 为 DELTA-002）
- 如无 delta 文件，delta_id = 001

### Step 2: 需求引导

向用户发起引导式提问：

**核心问题**：
1. `{{feature_name}}` 功能要解决什么问题？
2. 涉及哪些用户角色？他们分别需要什么？
3. 成功的标准是什么？（尽可能量化）

**可选深入**（视复杂度）：
4. 有哪些边界情况或异常流程？
5. 与现有系统的交互点？

### Step 3: 用户故事生成

基于用户回答，生成用户故事（As a... I want... so that...）和验收标准（Given/When/Then）。

**规则**：
- 每个用户故事 2-4 条验收标准
- 验收标准必须可测试（避免"快速"、"友好"等模糊词）
- 标注优先级（P0 = 必须有，P1 = 应该有，P2 = 可以有）

### Step 4: 术语一致性检查

对照 `steering/ubiquitous-lang.md` 检查：
- spec 中使用的术语是否与统一语言一致
- 如有新术语，标记为「待确认」并提示用户补充到 ubiquitous-lang.md
- 不一致的术语需与用户确认并修正

### Step 5: 填充模板

读取 `assets/templates/spec.md`，替换所有 `{{ }}` 占位符：

| 占位符 | 来源 |
|--------|------|
| `{{feature_name}}` | 用户指定的功能名 |
| `{{feature_summary}}` | 功能概述（1-2 句） |
| `{{story_N_title}}` | 用户故事标题 |
| `{{actor}}` | 用户角色 |
| `{{action}}` | 用户动作 |
| `{{benefit}}` | 用户收益 |
| 验收标准表格 | Given/When/Then 详情 |

模式条件分支：
- 增量模式 → 使用 `spec-delta.md` 模板，写入 `specs/{{feature_name}}/spec-delta-{{delta_id}}.md`
- 全量模式 → 使用 `spec.md` 模板，写入 `specs/{{feature_name}}/spec.md`

### Step 6: 更新状态

更新 `specs/{{feature_name}}/STATUS.md`（使用 `assets/templates/status.md`）：
- `{{current_phase}}` = Phase 2
- `{{phase0_status}}` = ✅ 完成
- `{{phase1_status}}` = ✅ 完成
- `{{phase2_status}}` = ✅ 完成
- 其余阶段 = ⬜ 待开始

## 输出

```
✅ Phase 2 (Specify) 完成: {{feature_name}}
- 用户故事: N 个
- 验收标准: M 条
- 术语一致性: {{term_issues}} 个待确认
- 优先级: P0={x}, P1={y}, P2={z}
- 文件: specs/{{feature_name}}/spec.md

下一步: spec-pm clarify {{feature_name}} 或 spec-pm design {{feature_name}}
```

## 约束

- 验收标准必须使用 Given/When/Then 格式
- 术语必须与 `ubiquitous-lang.md` 一致，新术语需经用户确认后再加入
- 如果 feature 跨越多个 Bounded Context，需在 spec 中标注并说明跨上下文交互方式